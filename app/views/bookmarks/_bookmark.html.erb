<% if post.bookmarked_by?(current_user) %>
  <%= link_to "ブクマ外す", post_bookmarks_path(post), method: :delete, remote: true %></td>
<% else %>
  <%= link_to "ブクマする", post_bookmarks_path(post), method: :post, remote: true %></td>
<% end %>