<% if post.user != current_user %>
  <% if post.bookmarked_by?(current_user) %>
    <%= link_to "外", post_bookmarks_path(post), method: :delete, remote: true %></td>
  <% else %>
    <%= link_to "ブ", post_bookmarks_path(post), method: :post, remote: true %></td>
  <% end %>
<% end %>